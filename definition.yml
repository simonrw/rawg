endpoints:
  - url: /record
    method: GET
types:
  - name: Record
    parameters:
      - name: foo
        type: int
  - name: Suggestion
    parameters:
      - name: text
        type: string
      - name: keyword
        type: string

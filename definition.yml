endpoints:
  - name: "records"
    url: /record
    method: GET
    returns: Suggestion
types:
  - name: Record
    parameters:
      - name: foo
        type: int
  - name: Suggestion
    parameters:
      - name: text
        type: string
      - name: keyword
        type: string
      - name: priority
        type: int
